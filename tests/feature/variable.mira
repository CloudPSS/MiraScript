{
  let outer = 'x';
  let x = 1;
  {
    let inner = 'y';
    let x = 2;
    {
      let x = 3;
      t_eq(x, 3);
      t_eq(global.x, nil);
      t_eq(global['x'], nil);
      t_eq(inner, 'y');
      t_eq(outer, 'x');
    }
    t_eq(x, 2);
    t_eq(global.x, nil);
    t_eq(global['x'], nil);
    t_eq(inner, 'y');
    t_eq(outer, 'x');
  }
  t_eq(x, 1);
  t_eq(global.x, nil);
  t_eq(global['x'], nil);
  t_eq(inner, nil);
  t_eq(outer, 'x');
}
t_eq(x, nil);
t_eq(global.x, nil);
t_eq(global['x'], nil);
t_eq(global[['x']], nil);
t_eq(outer, nil);
t_eq(inner, nil);
