let mut cj = 0;
let cjr = loop {
  let mut ci = 0;
  let cir = loop {
    ci += 1;
    if ci == 2 {
      break ci; // 跳出内层循环
    }
  };
  t_eq(cir, 2); // 内层循环返回的值
  cj += 1;
  if cj == 3 {
    break cj; // 跳出外层循环
  }
  continue;
  t_never();
};

t_eq(cjr, 3); // 外层循环返回的值
