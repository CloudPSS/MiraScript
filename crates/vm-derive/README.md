# mirascript-vm-derive

This crate implements the `mira` attribute macro and the `MiraRecord` and
`MiraArray` derive macros used by `mirascript-vm`. Applications should normally
import these macros from `mirascript` or `mirascript-vm` instead of depending on
this implementation crate directly.
