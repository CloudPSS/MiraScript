# mirascript-vm-derive

This crate implements the `MiraRecord`, `MiraArray` derive
macros used by `mirascript-vm`. Applications should normally import these macros
from `mirascript` or `mirascript-vm` instead of depending on this implementation
crate directly.
