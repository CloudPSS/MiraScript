{
  "name": "mira",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "type": "module",
  "scripts": {
    "prepare": "husky || echo Failed to initialize husky",
    "format": "prettier --cache --cache-strategy content --ignore-path .gitignore --write .",
    "lint": "eslint --cache --cache-location node_modules/.cache/eslint/ --cache-strategy content"
  },
  "prettier": "@cloudpss/prettier-config",
  "commitlint": {
    "extends": "@cloudpss"
  },
  "devDependencies": {
    "@cloudpss/commitlint-config": "^3.0.23",
    "@cloudpss/eslint-config": "^3.0.23",
    "@cloudpss/lint-staged-config": "^3.0.23",
    "@cloudpss/prettier-config": "^3.0.23",
    "@cloudpss/tsconfig": "^3.0.23",
    "@types/node": "^24.0.10",
    "better-typescript-lib": "^2.11.0",
    "esbuild": "^0.25.5",
    "husky": "^9.1.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
