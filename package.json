{
  "name": "mirascript",
  "version": "0.1.17",
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "type": "module",
  "scripts": {
    "prepare": "husky || echo Failed to initialize husky",
    "format": "prettier --cache --cache-strategy content --ignore-path .gitignore --write .",
    "lint": "eslint --cache --cache-location node_modules/.cache/eslint/ --cache-strategy content"
  },
  "prettier": "@cloudpss/prettier-config",
  "commitlint": {
    "extends": "@cloudpss"
  },
  "devDependencies": {
    "@cloudpss/commitlint-config": "^3.0.30",
    "@cloudpss/eslint-config": "^3.0.30",
    "@cloudpss/lint-staged-config": "^3.0.30",
    "@cloudpss/prettier-config": "^3.0.30",
    "@cloudpss/tsconfig": "^3.0.30",
    "better-typescript-lib": "^2.12.0",
    "esbuild": "^0.27.0",
    "husky": "^9.1.7",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
