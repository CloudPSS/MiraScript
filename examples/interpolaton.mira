let a = 1;
let b = (x: 2, y: 3);

"a is $a; b is ${b}; 1 + 2 is $(1 + (2)); b distance from origin is ${{
  let (:x, :y) = b;
  let d = sqrt(x * x + y * y);
  d
}}";

@"1 + 1 = $(1 + 1)"@;
@@"2$ + 2$ = $$(2 + 2)$"@@;
@"$if $import $is are not allowed"@;
@"$nil $true $nan $inf are allowed"@;
