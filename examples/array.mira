let array = [1, 2, 3, 10..20, ..[21, 22, 23], 24, 25, 30..<40];

let filtered = array::filter(fn {
    if it < 10 {
        return false;
    }
    if it > 20 {
        return false;
    }
    true
});

(array, filtered) // Return both the original and filtered arrays for inspection
