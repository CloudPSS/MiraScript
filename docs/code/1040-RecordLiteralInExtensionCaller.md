# RecordLiteralInExtensionCaller

等级：**错误**

意外的记录字面量；此处需要分组表达式

## 如何修复

扩展调用位置的单层括号会被解析为记录字面量；这里应放入分组后的可调用表达式。

### 修改前

```mira
1::(call: fn (x) { x })();
```

### 修复后

```mira
1::(fn (x) { x })();
```
