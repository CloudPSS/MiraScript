interface:
  display_name: 'MiraScript CLI'
  short_description: 'Inspect, run, validate, and format MiraScript code'
  default_prompt: 'Use $mirascript-cli to inspect the manual, then run and validate this MiraScript code.'
